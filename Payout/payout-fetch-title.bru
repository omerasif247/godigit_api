meta {
  name: payout/fetch-title
  type: http
  seq: 2
}

get {
  url: {{base_url}}/pay-out/fetch-title
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

settings {
  encodeUrl: true
}
